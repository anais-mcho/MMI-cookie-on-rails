<%= form_with(model: recipes_list) do |form| %>
  <% if recipes_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(recipes_list.errors.count, "error") %> prohibited this recipes_list from being saved:</h2>

      <ul>
        <% recipes_list.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
